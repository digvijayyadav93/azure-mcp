@{
    # Azure subscription and shared resource location
    SubscriptionId          = "00000000-0000-0000-0000-000000000000"
    Location                = "eastus"
    ResourceGroupName       = "rg-customer-data-dev"

    # Azure Functions resources. Both names must be globally unique.
    StorageAccountName      = "stcustomerdata12345"
    FunctionAppName         = "func-customer-data-12345"
    PythonVersion           = "3.13"

    # Direct Azure SQL mode: keep UseApi=false and provide a managed-identity
    # connection string. Leave SqlConnectionString empty to use local SQLite.
    # For a separate DB API, set UseApi=true, set DbApiBaseUrl, and leave the
    # SQL connection string empty.
    UseApi                   = $false
    DbApiBaseUrl             = ""
    SqlConnectionString      = "Server=your-server.database.windows.net;Database=your-database;Authentication=ActiveDirectoryMSI;Encrypt=yes;TrustServerCertificate=no;Connection Timeout=30;"

    # Microsoft Foundry resources. FoundryResourceName must be globally unique.
    FoundryResourceName      = "foundry-customer-data-12345"
    FoundryProjectName       = "customer-data-project"

    # Change these values if the model/version/SKU is unavailable in the region.
    ModelDeploymentName      = "gpt-5-mini"
    ModelName                = "gpt-5-mini"
    ModelVersion             = "2025-08-07"
    ModelFormat              = "OpenAI"
    ModelSkuName             = "GlobalStandard"
    ModelSkuCapacity         = 10

    # Agent and authenticated MCP connection
    AgentName                = "customer-data-agent"
    McpConnectionName        = "customer-data-mcp-connection"
    SkipAgentSmokeTest       = $false
}
